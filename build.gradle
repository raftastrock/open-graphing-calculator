apply plugin: "java"
apply plugin: "jetty"
apply plugin: "war"

repositories {
	mavenCentral()
}

dependencies {
    compile 'com.udojava:EvalEx:1.0'
    providedCompile "javax.servlet:javax.servlet-api:3.0.1"
}